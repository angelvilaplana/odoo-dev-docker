#!/usr/bin/env bash

source env/odoo.env

DATABASE=$1

bin/cliodoo /opt/odoo/odoo/odoo-bin shell \
    -d ${DATABASE} \
    --xmlrpc-port 8001 \
    --db_port 5432 \
    --db_host ${HOST} \
    --db_user ${USER} \
    --db_password ${PASSWORD} \
    --workers=0
