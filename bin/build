#!/usr/bin/env bash

ODOO_USER_ID=$(id -u)
POSTGRES_USER_ID=$(id -u)

bin/docker-compose build --build-arg POSTGRES_USER_ID=$POSTGRES_USER_ID db
bin/docker-compose build --build-arg ODOO_USER_ID=$ODOO_USER_ID odoo
bin/docker-compose up -d --remove-orphans "$@"
